# Spring Cloud for AWS Parameter Store & Integration Secreters Manager Integration
spring:
  config:
    import:
      - 'aws-parameterstore:/config/common/'
      - 'optional:aws-parameterstore:/config/sample-backend/'
      - 'aws-secretsmanager:/secrets/database-secrets/?prefix=spring.datasource.'
management:
  endpoints:
    web:
      discovery:
        enabled: false
        
logging:
  level:
    org.springframework: INFO
    org.springframework.jdbc: INFO
#    com.example: DEBUG
    com.example: INFO
#    software.amazon.awssdk: DEBUG